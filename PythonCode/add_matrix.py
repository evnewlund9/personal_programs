import create_matrix

def matrixSum(A,B):
    for row in A:
        for i in row:
            C[row][i] = A[row][i] + B[row][i]
    for row in C:
        for i in result:
            print(i, end = " ")
        print()

def sizeMatch(A,B):
    if length(A) != len(B):
        return True
    for row in A:
        if length(row) != lenth(B[row])

def main():
    A = create_matrix()
